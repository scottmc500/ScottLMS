# ScottLMS Terraform Variables Example
# Copy this file to terraform.tfvars and update with your actual values

# AWS Configuration (Optional - uses defaults if not specified)
# aws_region = "us-east-1"
# project_name = "scottlms"
# domain_name = "scottlms.com"

# DocumentDB Configuration (Optional - uses defaults if not specified)
# documentdb_instance_class = "db.t3.medium"
# documentdb_instance_count = 1

# Application Configuration (Optional - uses defaults if not specified)
# image_tag = "latest"

apiVersion: v1
kind: ConfigMap
metadata:
  name: scottlms-config
  namespace: scottlms
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "info"
  API_V1_STR: "/api/v1"
  PROJECT_NAME: "ScottLMS"
  DATABASE_NAME: "scottlms"
  K8S_NAMESPACE: "scottlms"
  # MongoDB Atlas URI will be set via secret
  # SECRET_KEY will be set via secret
